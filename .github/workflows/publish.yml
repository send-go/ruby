name: Publish to RubyGems

# vX.Y.Z 태그 push 또는 수동 실행 시 RubyGems 로 배포합니다.
# RubyGems Trusted Publishing(OIDC) 사용 — 장기 토큰 불필요.
# 사전 조건: rubygems.org 에 이 gem 의 Trusted Publisher(GitHub Actions, 이 repo, publish.yml) 등록.
on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      id-token: write   # OIDC (Trusted Publishing)
      contents: read
    steps:
      - uses: actions/checkout@v4
      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: "3.3"
      - name: RubyGems 자격증명 설정 (OIDC)
        uses: rubygems/configure-rubygems-credentials@v2.1.0
      - name: gem 빌드
        run: gem build sendgo.gemspec
      - name: RubyGems 배포
        run: gem push sendgo-*.gem
